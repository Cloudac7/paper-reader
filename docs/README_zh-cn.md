# Paper Reader Agent (论文智能解析助手)

**Paper Reader** 是一个基于 Agent 的科研论文深度解析系统。它结合了 **CrewAI** 的编排能力与 **OpenDataLab MinerU** 的解析能力，模拟科研人员的阅读习惯（略读、精读、批判性评估、总结），从 PDF 中提取高质量的结构化信息。

## ✨ 核心功能

*   **高保真解析**: 使用 [MinerU](https://github.com/opendatalab/MinerU) (Magic-PDF) 将 PDF 转换为结构化 Markdown，精准还原公式、表格和文档布局。
*   **多 Agent 协作**:
    *   **略读专家 (Skimmer)**: 快速提取核心贡献和逻辑大纲。
    *   **结构分析师 (Scanning Specialist)**: 利用语义向量搜索进行技术深挖（自动使用英文关键词检索以确保准确性）。
    *   **评审专家 (Critical Reviewer)**: 验证论文主张并寻找逻辑漏洞。
    *   **总结专家 (Summarizer)**: 将所有分析整合为一份专业的最终报告。
*   **多语言输出**: 支持指定生成报告的语言（默认为中文），同时在内部处理中保持英文检索以优化性能。
*   **过程可追溯**: 自动保存每个 Agent 的中间输出文件，便于核查。

## 🛠️ 安装说明

1.  **前置要求**:
    *   Python >= 3.13
    *   [uv](https://github.com/astral-sh/uv) (推荐用于依赖管理)
    *   **Mineru 环境**: 确保已安装 Mineru。默认情况下，系统会在 `mineru-env/bin/mineru` 查找可执行文件。你可以通过环境变量 `MINERU_BIN_PATH` 自定义路径。

2.  **克隆并同步**:
    ```bash
    git clone https://github.com/your-username/paper-reader.git
    cd paper-reader
    uv sync
    ```

3.  **配置环境**:
    在根目录创建 `.env` 文件：
    ```ini
    # 选择你的 LLM 提供商
    DEEPSEEK_API_KEY=sk-
    # 或
    ANTHROPIC_API_KEY=sk-
    # 或
    OPENAI_API_KEY=sk-

    # 可选：如果 Mineru 不在默认路径，请配置此项
    # MINERU_BIN_PATH=/path/to/mineru
    ```

## 🚀 使用方法

运行主程序并指定 PDF 文件路径：

```bash
# 默认模式 (生成中文报告)
uv run python src/main.py papers/demo.pdf

# 生成英文报告
uv run python src/main.py papers/demo.pdf --language English

# 生成日文报告
uv run python src/main.py papers/demo.pdf -l Japanese
```

## 📂 输出结构

所有结果将保存在 `output/` 目录下：
*   `{paper}_1_skimming.md`: 研究摘要卡片 (略读结果)。
*   `{paper}_2_deep_dive.md`: 方法论与实验详解 (精读结果)。
*   `{paper}_3_validation.md`: 批判性评估报告。
*   `{paper}_4_summary.md`: **最终综合总结报告**。

## 📄 许可证

本项目基于 MIT 许可证开源 - 详情请参阅 [LICENSE](LICENSE) 文件。